data:
  resource.customization.health.ConfigMap: |
    hs = {}
    hs.status = "Healthy"
      if obj.data.type == "Ready" and condition.status == "True" then
        hs.status = "Healthy"
        hs.message = condition.message
        return hs
        end