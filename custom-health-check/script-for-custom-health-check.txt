data:
  resource.customization.health.ConfigMap: |
    hs = {}
    hs.status = "Healthy"
      if obj.data.CUSTOM_HEADER == "Customized" then
        hs.status = "Degrated"
        hs.message = "Use different Header"
      end
    return hs